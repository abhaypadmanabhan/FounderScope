---
name: agents-done
description: Collect completed work from every agent worktree, verify it, merge accepted work into dev in safe order, run tests/gates, produce a testable build, and emit a manual QA checklist.
---

# agents-done

Read and follow the full workflow file at `.claude/commands/agents-done.md` (repo root), exactly and
in order. Treat any user-supplied arguments as the workflow's `$ARGUMENTS`.
