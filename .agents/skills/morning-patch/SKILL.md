---
name: morning-patch
description: Plan the day's patch — inspect repo, prioritize issues, write briefs, create per-agent worktrees off dev, emit copy-paste agent prompts. Never writes product code.
---

# morning-patch

Read and follow the full workflow file at `.claude/commands/morning-patch.md` (repo root), exactly and
in order. Treat any user-supplied arguments as the workflow's `$ARGUMENTS`.
